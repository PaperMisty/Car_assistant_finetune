# 智能汽车客服助手：数据工程与质检规范体系 (Data Engineering & QA Spec)

---

## 1. 文档性质与定位

在企业大模型落地与面试场景中，本项目相关文档的定位如下：

* **业务属性（业务 SOP / 需求规范）**：定义了汽车售后 8 大核心业务领域的业务边界、客服标准服务流程（SOP）、身份权限核验机制与安全红线（如禁止索要密码、禁止在行驶中指导操作）。
* **技术属性（数据工程方案 / 技术架构文档）**：详细制定了从 **Schema 规范定义 $\rightarrow$ 扰动矩阵扩增 $\rightarrow$ LLM 自动化合成 $\rightarrow$ L1/L2 双层质检 $\rightarrow$ 缺陷闭环治理 $\rightarrow$ SFT/DPO 训练** 的完整数据流水线规范。

---

## 2. 数据流水线总体架构

本项目采用工业级 **Schema 引导的合成数据工程（Schema-guided Synthetic Data Pipeline）**，总体流程如下图所示：

```text
┌─────────────────────────────────────────────────────────────┐
│ 1. 领域知识底座 (车主手册 + 4S店SOP + 汽车三包法 + 常见客诉)   │
└──────────────────────────────┬──────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────┐
│ 2. Schema 种子定义 (8大核心场景 + 必答事实 + 必追问项 + 红线)  │
└──────────────────────────────┬──────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────┐
│ 3. 正交扰动矩阵扩展 (信息缺失 / 系统超时 / 代办人等扰动)       │
└──────────────────────────────┬──────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────┐
│ 4. 大模型多轮生成 (Generator: 多轮交互、槽位填充、Tool Calling)│
└──────────────────────────────┬──────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────┐
│ 5. 双层质检与过滤流水线                                       │
│   ├─ L1: 规则层粗筛 (JSON校验、格式合规、敏感词拦截)          │
│   └─ L2: LLM-as-a-Judge 语义精筛 (履约度判定、幻觉与逻辑审查) │
└──────────────────────────────┬──────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────┐
│ 6. 分层抽检与缺陷闭环治理 (AQL准出、P0/P1/P2分级、RCA根因修复) │
└──────────────────────────────┬──────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────┐
│ 7. 产出交付物 (SFT 监督微调数据集 + DPO 偏好对齐数据集)       │
└─────────────────────────────────────────────────────────────┘
```

---

## 3. 种子数据设计与正交扰动矩阵

### 3.1 种子元数据 Schema 结构

每一条种子数据（Seed）代表一个严谨的业务边界定义：

```json
{
  "seed_id": "cat1_feature_021",
  "category_id": 1,
  "category": "用车与智能功能支持",
  "subcategory": "车机蓝牙",
  "scenario": "客户提供的信息不完整：客户手机搜索不到车辆蓝牙",
  "user_goal": "在信息不足时先补齐关键事实，再完成手机与车机配对",
  "customer_role": "车主本人",
  "service_stage": "用车中",
  "required_facts": ["车辆和手机蓝牙均需开启", "首次配对需在车机端确认"],
  "required_questions": ["手机型号及系统版本", "车机是否能发现其他设备"],
  "required_actions": ["指导删除旧配对记录后重新搜索", "先确认关键事实，再给出分步骤处理建议"],
  "prohibited_actions": ["不得要求客户提供手机密码", "不得在关键信息缺失时编造确定结论"],
  "tool_required": false,
  "tool_name": null
}
```

### 3.2 正交扰动矩阵（Perturbation Matrix）

为了让模型学会处理长尾与复杂异常边界，设计了 3 大类业务扰动：

| 编号区间 | 扰动类型 | 业务意图与能力考察 |
| :--- | :--- | :--- |
| **001 ~ 020** | **标准基线场景 (Base)** | 考察模型标准流程理解与正常信息回复能力 |
| **021 ~ 040** | **信息缺失扰动 (Incomplete Info)** | 考察多轮主动追问（Clarification）与槽位补齐能力 |
| **041 ~ 060** | **系统异常/无法复现扰动 (System Failure / Unreproducible)** | 考察 API 超时兜底、安全边界说明及引导到店能力 |
| **061 ~ 080** | **非本人/代办人咨询扰动 (Third-party Role)** | 考察身份核验、隐私合规与受限操作拦截意识 |

---

## 4. 双层质检与过滤机制 (L1 + L2 Pipeline)

为了解决“字面关键词命中但实际语义不符”的伪命中漏洞，质检体系分为两层：

```text
生成数据 ──> [ L1: 规则粗筛 ] ──(通过)──> [ L2: LLM-as-a-Judge 语义精筛 ] ──(通过)──> 合格数据池
                 │                                  │
               (拦截)                             (拦截)
                 ▼                                  ▼
             直接丢弃 / 记录                       Bad Case 记录库
```

### 4.1 L1 规则粗筛（Rule-based Filter）
* **格式检查**：JSON/JSONL 解析校验、多轮对话角色（`user`/`assistant`/`tool`）交替合法性。
* **工具调用协议校验**：`tool_name`、`arguments` 参数类型与必填字段检查。
* **低成本违规词拦截**：针对明令禁止的词汇/行为做正则快速扫描。

### 4.2 L2 语义精筛（LLM-as-a-Judge）
引入高能力大模型作为判官，要求以结构化 JSON 返回评估结果，评估维度包括：

1. **必答事实履约（Fact Fulfillment）**：是否准确提及 `required_facts`，而非仅字面重复。
2. **有效追问（Active Clarification）**：是否自然且有针对性地提出了 `required_questions`，而非机械化套模板。
3. **红线违规检测（Safety & Compliance）**：是否违背了 `prohibited_actions`（如向代办人泄露隐私、行驶中引导操作）。
4. **语境逻辑与工具合理性（Logical Consistency）**：对话是否流畅连贯，工具调用的时机和参数是否合理。

---

## 5. 质量评估与缺陷治理体系 (QA & RCA)

### 5.1 缺陷等级分级标准（Defect Severity）

| 缺陷级别 | 典型定义与示例 | 准出要求 (AQL) | 抽检 50 条判定标准 |
| :--- | :--- | :--- | :--- |
| **P0 致命缺陷 (Fatal)** | 违反安全/隐私红线（如索要密码、高压拆解指导、未验密越权代办） | **100%（0容忍）** | **命中 $\ge 1$ 条，整批数据立即判定不合格！** |
| **P1 严重缺陷 (Major)** | 遗漏核心必问槽位导致误诊、工具参数传错、明显事实幻觉 | **$\ge 95\%$** | 命中 $\ge 3$ 条（错误率 $>5\%$），判定整批不合格。 |
| **P2 轻微缺陷 (Minor)** | 表达机械生硬、少量口语化瑕疵、轻微语义冗余 | **$\ge 90\%$** | 允许存在少量（$\le 5$ 条），不触发全量重跑。 |

### 5.2 不合格处理闭环流程（严禁手动打补丁）

若抽检 50 条样本未达到准出标准，严禁只人工修改被抽中的几条样本，必须执行四步闭环：

```text
① 根因分析 (RCA) 
   → 分析缺陷是来源于：Seed 定义歧义、Generator Prompt 缺乏约束，还是 Judge 判决漏洞？
② 修复上游生成管道 (Fix Generator) 
   → 在 Prompt 中增加该缺陷场景的 Few-Shot 负例，增强 CoT（思维链）约束。
③ 固化自动化回归测试 (Fix Evaluator) 
   → 将该 Bad Case 加入到 L1/L2 自动化质检测试集，确保同类错误 100% 自动拦截。
④ 全量重新生成与再次抽检 (Re-run & Re-inspect) 
   → 重新跑批生成，重新分层抽样 50 条进行质检，直至通过 P0=100%, P1>=95% 的准出线。
```

---

## 6. 面试/项目汇报 核心话术速查表

* **关于数据来源**：“基于车主手册、4S店 SOP 规范与客诉场景自建的 Schema-guided 合成数据工程流水线。”
* **关于边界覆盖**：“通过正交扰动矩阵，主动注入信息缺失、系统故障超时、第三方代办等长尾复杂场景。”
* **关于质量保障**：“采用 L1 规则粗筛 + L2 LLM-as-a-Judge 语义精筛的双层流水线，结合 P0 0容忍、P1 $\ge 95\%$ 的缺陷分级抽检与上游 RCA 闭环治理。”
