[project]
name = "car-assitant"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "detect-secrets>=1.5.0",
    "dotenv>=0.9.9",
    "openai>=3.6.0",
    "pre-commit>=4.6.2",
]

[dependency-groups]
dev = [
    "pre-commit>=4.6.2",
]
