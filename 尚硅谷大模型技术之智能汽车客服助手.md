## 第1章 项目概述

汽车售后服务中，客户的问题涉及车辆使用、保养政策、故障诊断、预约维修、费用咨询、道路救援和投诉处理等多个环节。传统客服主要依赖人工经验，容易出现响应速度慢、回答口径不统一、遗漏
  关键信息以及处理边界不清晰等问题。

本项目希望通过 SFT 和 DPO，将通用大语言模型微调为一个面向汽车售后场景的智能客服助手，使模型能够理解客户的真实诉求，按照规范的业务流程进行追问、判断、解释和引导。

以下是该场景的真实案例：

| 案例                                        | 来源                   | 说明                                   |
| ------------------------------------------- | ---------------------- | -------------------------------------- |
| Impel 微调 LLM 服务汽车经销商客户互动       | AWS 官方博客（2025.6） | "微调 + 汽车客服"标杆案例              |
| 长安汽车"智能小安"AI 客服                   | 京东 AI 赋能（2025.6） | 客服体验分跃升 78.9%                   |
| 丰田 × 中关村科金大模型语音智能体           | 2025                   | 老客运营触达接通率 60%，售后回访智能化 |
| 4S 店 AI 语音数字员工售后回访               | 2025                   | 回访效率提升 400%                      |
| 一汽丰田专家级汽车服务智能客服              | 2025                   | "AI 驱动安全护航"（故障分诊类功能）    |
| 现代汽车 × Google Cloud AI 技术助理         | 2025                   | 症状描述 → 初步判断 + 下一步建议       |
| 腾讯云 ADP 智能体                           | 2025                   | 故障排查 + 维修店推荐                  |
| 4S 店本地 AI 生成客户关怀话术与投诉应对预案 | 2025                   | 话术生成直接落地 4S 店                 |

## 第2章 数据准备

### 2.1 种子数据分类

本项目已提供种子数据，可以在资料中获取。本项目种子数据，包含8种不同的场景：

| 能力     | 类别编号 | 类别                     | 标准流程                                                     | SFT重点                                            | DPO重点                                                      |
| -------- | -------- | ------------------------ | ------------------------------------------------------------ | -------------------------------------------------- | ------------------------------------------------------------ |
| 知识解释 | 1        | 用车与智能功能支持       | 识别车型与软件版本 → 追问现象和发生条件 → 给出分步排查 → 判断是否恢复 → 未恢复则远程诊断、预约或转工程师 | 多轮追问、步骤清晰、不给未经验证的确定结论         | 有针对性的排查优于泛泛而谈；适时转服务优于反复让用户重启     |
| 知识解释 | 2        | 保养、质保与服务政策     | 确认车型/VIN/购车日期/里程 → 查询对应政策版本 → 判断适用条件 → 解释包含与排除项 → 给出查询、预约或审核入口 | 条件化回答、政策来源、价格与权益边界               | 完整说明适用条件，不把单一品牌政策说成行业通用规则           |
| 风险决策 | 3        | 故障预诊断与安全分流     | 确认人员和车辆所处环境 → 追问关键症状 → 红/黄/绿风险分级 → 给出当前动作和禁止事项 → 预约、远程诊断或转救援 | 安全前置、信息不足先追问、明确升级条件             | 保守且有条件的判断优于武断建议                               |
| 业务办理 | 4        | 预约与服务受理           | 识别客户与车辆 → 收集服务项目和必要槽位 → 查询网点/工位/服务能力 → 给出可选方案 → 客户确认 → 创建或修改预约 → 返回预约编号和注意事项 | 多轮槽位收集、工具调用、执行前确认、系统失败处理   | 有替代方案的回复优于只说“约满了”；没有系统结果时不得声称“已预约成功” |
| 业务办理 | 5        | 维修过程、费用与交车     | 查询工单 → 返回真实维修节点 → 解释方案与费用 → 增项须由客户确认 → 延期时同步原因和替代安排 → 完工后通知支付/取车 → 回访 | 准确引用工单结果、费用透明、不能虚构进度和完工时间 | 事实说明 + 选择方案 + 明确下一次更新时间，优于空泛道歉或未经授权承诺补偿 |
| 业务办理 | 6        | 道路救援、事故与保险     | 先确认人员是否受伤和车辆是否处于危险位置 → 必要时报警/急救 → 获取定位、VIN 和联系方式 → 判断救援类型 → 查询权益与费用 → 客户确认 → 下发救援单 → 跟踪进度 → 转事故维修/保险 | 人身安全优先、字段采集、紧急分流、费用边界         | 先安全后业务                                                 |
| 关系维护 | 7        | 投诉、质量争议与升级处理 | 接住情绪但不预判责任 → 复述并确认事实 → 查询历史工单 → 明确客户诉求 → 建立投诉单 → 给出责任人和回复时限 → 升级调查 → 持续同步 → 方案确认 → 关闭并回访 | 共情、事实边界、投诉闭环和持续跟进                 | 不推诿但也不越权认责；具体处理路径优于模板化安抚；补偿必须来自授权结果 |
|          | 8        | 主动关怀、回访与客户运营 | 确认触达目的和授权 → 说明身份与原因 → 提供必要信息 → 收集反馈/确认行动 → 尊重拒绝和退订 → 记录结果 → 必要时转人工或创建业务单 | 区分服务、安全和营销通知；表达克制，保护隐私       | 目的明确、不过度营销、允许拒绝和退订的回复优于强推销话术     |

当中种子数据：已经区分了三大类：

- train: 训练集，用于训练的种子数据，所在目录：data/v2/seeds/train
- validation: 验证集，用于训练过程中评估模型、选择最好的模型，所在目录：data/v2/seeds/validation
- final_test: 最终测试集，最终测试集，只在训练和调参全部结束后使用，用来做最终模型对比，所在目录：data/v2/seeds/final_test

### 2.2 种子数据结构解析

当前种子数据结构（schema信息）中，各个字段含义如下所示：

| schema_name | schema_type | schema_info |
| ----------- | ----------- | ----------- |
| `seed_id` | string | 种子的唯一编号，例如 `cat1_feature_001` 表示第1类业务的第1个种子。 |
| `split` | enum | 数据集划分：`train` 训练集、`validation` 验证集、`final_test` 最终测试集。 |
| `category_id` | integer | 一级业务类别编号，范围为1到8。 |
| `category` | string | 一级业务类别名称，必须与 `category_id` 对应。 |
| `subcategory` | string | 一级类别下的具体业务主题。 |
| `scenario` | string | 当前发生的业务场景。 |
| `user_goal` | string | 客户希望达到的目标。 |
| `customer_role` | string | 咨询人的身份，例如车主本人、家属或代办人。 |
| `service_stage` | string | 当前服务阶段，例如用车中、预约前或维修中。 |
| `required_facts` | array[string] | 客服回答必须覆盖的事实信息。<br />(这是LLM需要具备的内化/外挂的知识,可以用system提词训练) |
| `required_questions` | array[string] | 客服需要向客户追问的关键信息。 |
| `required_actions` | array[string] | 客服必须执行或建议客户执行的动作。 |
| `prohibited_actions` | array[string] | 客服禁止执行、建议或承诺的行为。 |
| `tool_required` | boolean | 是否必须查询业务系统或调用工具，取值为 `true` 或 `false`。 |
| `tool_name` | string/null | 需要调用的工具名称；不需要工具时为 `null`。 |

### 2.3 数据构建流程

```text
① 为 8 类场景整理独立业务种子
   → ② 先按 seed_id 划分 train / validation / final-test
   → ③ 只在各自集合内生成单轮、多轮、工具调用和异常变体
   → ④ 规则校验（格式/槽位/安全项/工具状态/长度/去重）
   → ⑤ 人工分层抽检与修订
   → ⑥ 生成 SFT 数据 → 训练 SFT
   → ⑦ 从 SFT 训练池构造 DPO 数据 → 训练 DPO
   → ⑧ 使用独立 final-test 和人工盲评复验
```

## 第3章 模型训练

本项目模型训练，可以自选模型来完成。

本项目，需要先通过SFT，来让模型学习基本的格式，再通过DPO来完成偏好对齐。

## 第4章 模型验证

在完成SFT后，需对模型进行评估，评估该模型基本指令遵循能力，在完成DPO后，需以SFT之后的模型为baseline，来评估DPO的模型效果。

## 第5章 阶段里程碑

| 里程碑名称 | 目标                                                         |
| ---------- | ------------------------------------------------------------ |
| 里程碑一   | 通过种子数据，构造SFT训练数据                                |
| 里程碑二   | 通过SFT训练，获取SFT后的模型                                 |
| 里程碑三   | 对SFT后的模型做评估                                          |
| 里程碑四   | 使用SFT后的模型进行真实采样，从中抽取较好的回答作为chosen，较差的回答作为rejected数据，构造DPO数据集<br />(如何评价chosen和rejected) |
| 里程碑五   | 通过DPO训练，获取DPO后的模型                                 |
| 里程碑六   | 对DPO后的模型做评估                                          |

### 5.1 里程碑一：构造 SFT 训练数据

#### 输入

项目提供的种子数据，其中主要使用 `split=train` 的数据进行训练数据构造。

#### 任务

基于种子数据构造适用于 SFT 的训练数据。

学生需要根据每条种子数据描述的业务场景，生成对应的用户问题和客服回答。可以对同一条种子数据进行适当扩写，例如改变用户表达方式、信息完整程度、问题描述顺序等，但扩写后的数据应保持原始业务规则不变。

生成的客服回答应尽可能满足：

1. 覆盖 `required_facts` 中要求说明的关键信息；
2. 根据需要向用户询问 `required_questions` 中的信息；
3. 给出 `required_actions` 中要求的处理建议；
4. 不得出现 `prohibited_actions` 中禁止的行为；
5. 当 `tool_required=true` 时，不应伪造业务系统查询结果。

最终将生成的数据整理为模型训练所需的数据格式，例如 `messages` 格式。

#### 输出

至少形成以下数据文件：

- `sft_train.jsonl`：SFT 训练数据；
- `sft_validation.jsonl`：SFT 验证数据。

建议同时保留数据生成脚本及生成 Prompt，便于后续修改和重新生成数据。

#### 验收标准

- 数据可以被后续 SFT 训练程序正常读取；
- 数据格式正确，不存在明显字段缺失或 JSON 格式错误；
- 用户问题与对应业务场景基本一致；
- 客服回答能够覆盖种子数据要求的主要业务规则；
- 不出现明显违反 `prohibited_actions` 的回答；
- 随机抽取部分数据进行人工检查，不存在大量重复、语义错误或明显低质量样本。

------

### 5.2 里程碑二：完成 SFT 模型训练

#### 输入

- 里程碑一生成的 `sft_train.jsonl`；
- SFT 验证数据；
- 自选的基础模型。

#### 任务

选择合适的基础模型，通过 SFT 让模型学习汽车售后客服场景中的：

- 基本回答方式；
- 业务流程；
- 信息追问方式；
- 回答格式；
- 不同类型问题的基本处理逻辑。

需要自行完成：

1. 数据加载与预处理；
2. Tokenizer 处理；
3. 训练参数配置；
4. LoRA、QLoRA 或其他训练方案配置；
5. 模型训练；
6. 训练过程中的 loss 和 validation loss 观察；
7. 模型及 Adapter 保存。

训练过程中应关注是否存在显存不足、训练 loss 异常、验证 loss 明显恶化等问题，并根据实际情况调整训练参数。

#### 输出

- SFT 后的模型或 LoRA Adapter；
- SFT 训练代码；
- 训练参数配置；
- 训练过程中的 loss 等训练记录。(这个可以注意用eval_loss+耐心早停机制, 每1/4进度保存一次模型)

建议保存为类似：

```
finetuned/sft_model/
```

#### 验收标准

- SFT 训练流程能够完整运行结束；
- 模型权重或 Adapter 能够正常保存和重新加载；
- 训练 loss 整体能够正常下降；
- 不存在 loss 为 `NaN` 等明显训练异常；
- 加载训练后的模型后能够正常进行文本生成；
- 对部分汽车客服测试问题进行人工测试后，相比原始模型已经表现出一定的业务客服特征。

------

### 5.3 里程碑三：评估 SFT 模型

#### 输入

- SFT 后的模型；
- `validation` 或专门构造的评估数据；
- 对应的业务规则信息。

#### 任务

使用vLLM对模型进行部署，再利用evalscope对 SFT 模型进行评估，判断模型是否真正学习到了基本的汽车客服能力。

评估内容可以重点关注：

1. 是否正确理解用户的问题；
2. 是否覆盖必须说明的 `required_facts`；
3. 是否进行了必要的 `required_questions` 追问；
4. 是否给出了正确的 `required_actions`；
5. 是否出现 `prohibited_actions`；
6. 回答是否完整、自然、清晰；
7. 是否存在编造业务政策、维修结果或系统查询结果的问题。

评估可以使用人工评估，也可以使用能力更强的模型作为 Judge 对模型回答进行评分。

应记录模型表现较差的案例，即 Bad Case，为后续 DPO 数据构造提供参考。(注意, 这个具体是做什么参考?? DPO不是已经有了rejected回答sample了么)

#### 输出

- SFT 模型评估结果；
- 各类型场景的评估得分或统计结果；
- SFT Bad Case；
- 简单的评估分析报告。

#### 验收标准

- 能够使用统一的评估流程批量测试模型；
- 测试数据不直接使用 SFT 训练数据；
- 可以得到可解释的评估结果，而不仅仅是观察几个模型回答；
- 能够总结 SFT 模型当前主要存在的问题；
- 能够整理出一批后续可以用于偏好优化的典型 Bad Case。

------

### 5.4 里程碑四：构造 DPO 偏好数据

#### 输入

- SFT 后的模型；
- SFT 阶段发现的 Bad Case；
- 原始 `train` 种子数据；
- 用于模型生成的用户 Prompt。

#### 任务

使用 SFT 后的模型对相同问题进行多次采样，获得多个不同回答，并从中构造 DPO 所需的偏好数据。

一种基本实现方式为：

1. 对同一个用户问题，让 SFT 模型通过一定的随机采样生成多个不同回答；
2. 根据业务规则、人工判断或 Judge 模型对多个回答进行比较；
3. 选择质量较好的回答作为 `chosen`；
4. 选择质量较差的回答作为 `rejected`；
5. 构造 DPO 数据集。

在判断回答优劣时，可以重点考虑：

- 是否遗漏 `required_facts`；
- 是否缺少必要追问；
- 是否进行了错误操作；
- 是否违反 `prohibited_actions`；
- 是否存在编造信息；
- 回答是否过度承诺；
- 回答是否啰嗦、模糊或缺少可执行建议。

应优先选择 chosen 与 rejected 存在明确质量差异的数据，而不是大量加入几乎没有区别的回答。

#### 输出

DPO 数据集，例如：

```
dpo_train.jsonl
```

每条数据能够表达：

- 用户输入；
- `chosen` 回答；
- `rejected` 回答。

同时建议保存：

- DPO 数据构造脚本；
- 模型采样结果；
- Judge 评分或人工筛选结果。

#### 验收标准

- DPO 数据能够被后续 DPOTrainer 或对应训练程序正常加载；
- chosen 和 rejected 均能够正确对应同一个用户问题；
- chosen 整体质量应明显优于 rejected；
- chosen 不应仅仅因为“回答更长”而被判定为更优；
- 数据中应包含一定数量来自 SFT Bad Case 的样本；
- 随机人工抽查后，大部分数据能够明确判断出 chosen 优于 rejected。

------

### 5.5 里程碑五：完成 DPO 模型训练

#### 输入

- SFT 后的模型；
- 里程碑四生成的 DPO 数据集；
- DPO 训练参数。

#### 任务

以 SFT 模型作为 DPO 阶段的初始模型，对构造好的偏好数据进行训练，使模型进一步学习：

- 什么样的回答更符合业务要求；
- 什么样的回答应该避免；
- 如何减少遗漏关键信息；
- 如何减少错误承诺和违规回答；
- 如何在多个可能回答中更加倾向于高质量回答。

学生需要完成：

1. DPO 数据加载；
2. DPOTrainer 或对应训练流程配置；
3. Reference Model 配置；
4. `beta`、learning rate、batch size 等参数配置；
5. 模型训练；
6. 训练日志观察；
7. DPO 模型保存。

#### 输出

- DPO 后的模型或 LoRA Adapter；
- DPO 训练代码；
- DPO 训练参数；
- DPO 训练日志。

建议保存为：

```
finetuned/dpo_model/
```

#### 验收标准

- DPO 训练能够正常完成；
- 模型或 Adapter 能够正常保存并重新加载；
- 训练过程中没有明显的 `NaN`、OOM 等异常；
- DPO 模型能够正常进行推理；
- 对部分偏好问题进行人工测试时，模型输出不存在明显的能力退化；
- 在部分 SFT Bad Case 上能够观察到一定改善。

------

### 5.6 里程碑六：评估 DPO 模型

#### 输入

- SFT 后的模型；
- DPO 后的模型；
- `final_test` 数据；
- 统一的模型生成参数和评估规则。

#### 任务

以 SFT 模型作为 baseline，对 DPO 模型进行最终评估。

对于相同的测试问题，分别使用：

- SFT 模型生成回答；
- DPO 模型生成回答。

然后通过人工评估或 Judge 模型比较两个模型的回答。

评估时应尽可能保证：

- 两个模型使用相同的测试问题；
- 使用相同或尽可能一致的生成参数；
- 使用统一的 Judge 和评分规则。

重点比较：

1. required facts 覆盖情况；
2. 必要追问是否完整；
3. required actions 是否正确；
4. prohibited actions 违规情况；
5. 编造信息情况；
6. 回答完整性；
7. 回答自然程度；
8. DPO 模型相对于 SFT 模型的胜率。

同时需要分析：

- 哪些问题经过 DPO 后得到改善；
- 哪些问题没有明显变化；
- 哪些问题出现了能力下降；
- 下一轮应该补充什么类型的数据。

#### 输出

最终项目至少应包含：

- SFT 模型评估结果；
- DPO 模型评估结果；
- SFT vs DPO 对比结果；
- DPO 相对于 SFT 的 Win Rate；
- 典型 Good Case；
- 典型 Bad Case；
- 最终项目分析报告。

#### 验收标准

- SFT 和 DPO 模型使用同一套最终测试数据完成评估；
- `final_test` 数据没有参与前面的模型训练和参数调整；
- 能够得到 SFT 与 DPO 的量化对比结果；
- 能够展示若干 DPO 优于 SFT 的具体案例；
- 能够展示并分析仍然存在的 Bad Case；
- 最终能够回答一个核心问题：

**DPO 是否在不明显破坏 SFT 基础能力的情况下，使模型更加符合汽车客服业务规则和回答偏好。**
