# 智能汽车客服助手 - 大模型与 vLLM 压力测试指南

本文档系统总结了智能汽车客服助手大模型在上线部署、服务化以及吞吐极限评估时的**完整压测方法论、核心 SLA 指标定义、官方工具实操与生产调优建议**。

---

## 一、压测核心背景与最佳实践定位

大模型推理与传统 Web 服务有本质区别：大模型推理分为 **Prefill（首字计算/计算密集型）** 与 **Decode（自回归生成/显存带宽密集型）** 两个阶段。

在工业界，推理运行方式严格区分为两种：

| 使用场景 | 推荐运行方式 | 核心诉求 | 典型应用 |
| :--- | :--- | :--- | :--- |
| **离线批量推理 (Offline Batch)** | **Python 原生脚本启动** (`from vllm import LLM`) | 追求**最大吞吐率 (Throughput)**，无 HTTP 网络损耗 | 离线生成评测答卷、批量数据标注、对齐税跑分 |
| **在线实时服务 (Online Serving)** | **独立 HTTP API 服务** (`vllm serve` / FastAPI) | 追求**极低首字延迟 (TTFT)** 与高并发稳定性 | 车机端 App、微信小程序、多轮客服对话后台 |

**压力测试的核心目标**：在保证车主交流流畅（首字不卡顿、打字机不掉帧）的前提下，摸清单卡/集群的**最大并发用户承载上限（Concurrency Limit）**与**显存极限边界**。

---

## 二、四大核心 SLA 性能指标定义

在汽车客服场景中，必须重点监控以下四大核心指标：

```
车主发送消息 ───────────► 看到第 1 个字 ───────────► 回答全部吐完 (结束)
    │                         │                          │
    └──────── TTFT ───────────┴────────── TPOT ──────────┘
    │                                                    │
    └───────────────── E2E Latency ──────────────────────┘
```

1. **TTFT (Time To First Token) 首字延迟**
   - **定义**：从车主按下发送键，到车机大屏吐出第一个文字的时间。
   - **客服业务红线**：**P90 < 600ms，P95 < 1000ms**（若超过 1.5 秒，车主会产生“卡死”感而重复发送）。
2. **TPOT (Time Per Output Token) / ITL (Inter-Token Latency) 词间延迟**
   - **定义**：流式生成过程中，相邻两个 Token/文字之间的生成间隔。
   - **客服业务红线**：**平均 < 30ms/字，P95 < 50ms/字**（人类舒适阅读速度约为 15~25 字/秒）。
3. **Throughput (Tokens/s) 整机吞吐率**
   - **定义**：GPU 在单位时间内并发生成的总 Token 数量。衡量 GPU 算力利用率与服务器成本。
4. **RPS (Requests Per Second) 与最大并发承载数**
   - **定义**：在延迟不发生陡增雪崩的情况下，单张 GPU 稳定支持的每秒最大请求数与同时在线车主数。

---

## 三、方法一：vLLM 官方标准基准压测（推荐首选）

vLLM 官方自带了极度精准的 Benchmark 压测套件，能够精确剥离网络干扰，输出 P50/P90/P99 延迟分位数。

### 1. 启动 vLLM 在线 API 服务（挂载 LoRA）

在 GPU 服务器（如 RTX 4090 / 5090）上以后台方式启动服务：

```bash
python -m vllm.entrypoints.openai.api_server \
    --model model/Qwen/Qwen3-8B \
    --tensor-parallel-size 1 \
    --gpu-memory-utilization 0.90 \
    --max-model-len 4096 \
    --enable-lora \
    --lora-modules sft_car=output/qwen_8b_lora_sft \
    --max-lora-rank 16 \
    --enable-prefix-caching \
    --trust-remote-code \
    --port 8000
```

> **核心参数说明**：
> - `--enable-prefix-caching`：**强烈推荐开启！** 自动缓存多轮对话中重复出现的 `System Prompt` 与历史工具描述，可让客服场景的首字延迟（TTFT）暴降 60%~80%！
> - `--max-lora-rank 16`：预分配 LoRA 显存池上限，与微调时的 $r=16$ 保持一致。

---

### 2. 运行在线并发压测 (`benchmark_serving.py`)

使用 vLLM 自带的真实在线服务压测脚本，模拟真实流量波峰：

```bash
# 模拟 30 个并发客户端、请求到达符合泊松分布 (10 req/s)，累计测试 200 条请求
python -m vllm.benchmarks.benchmark_serving \
    --backend vllm \
    --model sft_car \
    --base-url http://localhost:8000 \
    --dataset-name sharegpt \
    --num-prompts 200 \
    --request-rate 10 \
    --max-concurrency 30 \
    --percentile-metrics "ttft,tpot,latency"
```

#### 压测输出关键看板样例：
```text
================ 压测性能评估看板 ================
总测试请求数: 200
请求吞吐率 (RPS): 9.82 req/s
输出 Token 吞吐率: 845.3 tokens/s
--------------------------------------------------
首字延迟 (TTFT):
  Mean:   245.3 ms
  P50:    198.1 ms
  P95:    520.4 ms   <-- 达标 (目标 < 800ms)
  P99:    780.2 ms
--------------------------------------------------
词间生成延迟 (TPOT):
  Mean:   24.1 ms    <-- 流畅度达标 (目标 < 30ms)
  P95:    31.5 ms
--------------------------------------------------
端到端请求耗时 (Latency):
  Mean:   2.85 s
  P95:    5.12 s
==================================================
```

---

### 3. 运行离线极限吞吐压测 (`benchmark_throughput.py`)

用于评估当完全不考虑网络、单卡全负荷跑批任务时，GPU 的极限吞吐（例如跑全量 10 万条数据的质检打分）：

```bash
python -m vllm.benchmarks.benchmark_throughput \
    --model model/Qwen/Qwen3-8B \
    --enable-lora \
    --lora-path output/qwen_8b_lora_sft \
    --dataset /path/to/ShareGPT_V3_unfiltered_cleaned_split.json \
    --num-prompts 500 \
    --gpu-memory-utilization 0.90 \
    --max-model-len 4096
```

---

## 四、方法二：使用 Locust 模拟真实车主多轮会话交互

如果需要测试全业务链路（例如网关校验、Pydantic 模式过滤、FastAPI 接口重试），可使用 Python 流行的高并发压测工具 **Locust**。

### 1. 编写 `locustfile.py`

在根目录下创建 `tests/locustfile.py`：

```python
import json
import random
from locust import HttpUser, between, task

# 模拟典型车主提问库
QUERIES = [
    "进隧道后大灯没有自动点亮，怎么回事？急赶飞机！",
    "锁车后后视镜没有自动折叠，会不会被蹭？",
    "用脚扫尾门感应区怎么完全没反应？",
    "车里PM2.5爆表显示180多，为什么净化器不启动？",
    "刚充好气表显怎么还显示2.1bar并亮黄灯？",
]


class CarAssistantUser(HttpUser):
    # 模拟车主输入与阅读思考间隔 (1 ~ 3 秒)
    wait_time = between(1, 3)

    @task
    def ask_car_question(self):
        query = random.choice(QUERIES)
        payload = {
            "model": "sft_car",
            "messages": [
                {
                    "role": "system",
                    "content": "你是智能汽车官方客服助手。回答必须专业、安全第一。",
                },
                {"role": "user", "content": query},
            ],
            "max_tokens": 512,
            "temperature": 0.3,
            "stream": True,  # 模拟流式打字机拉取
        }

        # 发起流式 SSE 压测
        with self.client.post(
            "/v1/chat/completions",
            json=payload,
            headers={"Content-Type": "application/json"},
            stream=True,
            catch_response=True,
        ) as response:
            if response.status_code == 200:
                # 遍历接收流式 chunks
                for line in response.iter_lines():
                    pass
                response.success()
            else:
                response.failure(f"HTTP 错误: {response.status_code}")
```

### 2. 启动 Locust 分布式或单机压测

```bash
# 启动压测 Web 控制台
locust -f tests/locustfile.py --host http://localhost:8000
```
打开浏览器访问 `http://localhost:8089`：
- 设置并发车主用户数（如：`Number of users = 50`）；
- 设置递增速率（如：`Ramp up = 5 users/s`）；
- 实时观察 RPS 曲线、95% 响应延迟与 HTTP 错误率。

---

## 五、压测避坑指南与极限调优秘籍

1. **启用前缀缓存（Prefix Caching）**
   - 车载客服助手通常有一段长达 200~500 字的固定 `system_prompt`（包含安抚规范、禁止行为、免责声明等）。
   - 启动时加上 `--enable-prefix-caching`，vLLM 会对所有会话的 System 提示词进行 KV Cache 共享重用，**首字延迟可降低 70% 以上**。

2. **自适应探测 LoRA 秩（避免预分配浪费显存）**
   - vLLM 初始化时会将 `max_lora_rank` 的显存槽位在启动时焊死。
   - 如果你的 LoRA 权重秩为 16，切勿无脑填 64 或 128，否则会浪费大量宝贵的 KV Cache 显存空间，导致并发吞吐暴跌。

3. **并发饱和度与 P99 延迟“陡增拐点”**
   - 压测时应逐步增加并发数（10 $\to$ 20 $\to$ 40 $\to$ 80）。
   - 当发现并发数从 40 增加到 50 时，**TTFT 从 400ms 突增到 3000ms**，说明此时系统的 KV Cache 已被占满，部分请求被迫进入 CPU 交换或等待队列（Queueing Delay）。
   - **该突增拐点（如 40 并发）即为单卡生产环境的准入保护上限（SLA Limit）**。
